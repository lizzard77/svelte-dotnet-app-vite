/**
 * Minimal Svelte 5 config for this project.
 * Adjust compilerOptions as needed; do not enable experimental flags unless desired.
 */
export default {
  compilerOptions: {
    // compatibility options can be set here if needed
    // example: compatibility: { componentApi: 4 }
  }
}
